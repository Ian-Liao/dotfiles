# dotfiles

Requirement
-----------

Choose zsh as your default shell:

	chsh -s $(which zsh)

What it contains?
----------------

zsh configuration

vim configuration

git configuration

macOS configuration

Homebrew formulae installation
